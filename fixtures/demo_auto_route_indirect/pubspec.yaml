name: demo_auto_route_indirect
description: >-
  auto_route fixture where the router references per-screen `route` members
  instead of declaring AutoRoute inline.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^9.0.0

flutter:
  uses-material-design: true
